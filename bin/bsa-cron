#!/bin/bash

if [ ! -t 0 ]
then
 exec >> /tmp/bsa-cron.log
 exec 2>&1
 echo '======================'
 date
fi

path=$(type -p $0)
path=${path%/*}

if [ -z "$path" -o ! -d "$path" ]
then
  echo could not find working dir
  exit 1
fi

. $path/init

. $bindir/checkfw

. $bindir/action
