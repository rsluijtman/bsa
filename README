bsa, block ssh attack

block addresses after several failed login attempts

get_addresses:
 started by rsyslogd, reads syslog output, records addresses


cronjobs

action
 checks recorded addresses and blocks them if appropriate

rm_addresses
 removes recorded addresses after certain time
